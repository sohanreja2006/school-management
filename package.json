{
  "name": "academix-saas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install": "cd backend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
